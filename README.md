# РАСТАМОЖЬ.РФ — калькулятор импорта

Vercel version with server-side Calcus API proxy.

Required Vercel environment variables:
- CALCUS_CLIENT
- CALCUS_KEY

The browser calls `/api/customs`; credentials never appear in frontend code.
